export const Title48 = ({ children, className }) => {
    return <p className={`${className} text-[48px]  font-bold leading-16 `}> {children} </p>
}

export const Title24 = ({ children, className }) => {
    return <p className={`${className} text-[24px]  font-base leading-9 `}> {children} </p>
}

export const Title32 = ({ children, className }) => {
    return <p className={`${className} text-[32px]  font-semibold leading-12 `}> {children} </p>
}

export const Title18 = ({ children, className }) => {
    return <p className={`${className} text-[18px]  font-medium leading-7 `}> {children} </p>
}

export const Title16 = ({ children, className }) => {
    return <p className={`${className} text-[16px]  font-normal leading-6 `}> {children} </p>
}

export const Title20 = ({ children, className }) => {
    return <p className={`${className} text-[20px]  font-normal leading-6 `}> {children} </p>
}